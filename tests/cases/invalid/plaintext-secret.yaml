# 非法用例 3：明文凭据
# 期望：被安全校验拦下，报错路径指向 environment.db.password
schema_version: 1

id: INVALID-PLAINTEXT-SECRET
title: 把密码写死在用例里的
module: demo
priority: P0
tags: [invalid, security]

environment:
  name: local
  db:
    host: 127.0.0.1
    port: 33306
    user: mtp
    database: mtp_test
    # 这里必须是 {{ secrets.xxx }}，明文一律拒绝
    password: SuperSecret123

steps:
  - id: query
    action: mysql.query
    args:
      sql: SELECT 1
      credentials: "{{ env.db }}"
    timeout_sec: 10
